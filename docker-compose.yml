services:
  orchestrator:
    image: ghcr.io/krinkuto11/acestream-orchestrator:latest
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    restart: on-failure
